#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, y, z;
    cin >> x >> y >> z;
    if (x >= 90 || x >= 90 || z >= 90)
    {
        cout << "beijing" << endl;
    }
    else
    {
        cout << "nanjing" << endl;
    }

    return 0;
}